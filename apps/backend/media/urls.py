"""
Media app URL configuration
"""

from django.urls import path
from . import views

urlpatterns = [
    # Media management endpoints will be added here
    # path("upload/", views.MediaUploadView.as_view(), name="media_upload"),
]
